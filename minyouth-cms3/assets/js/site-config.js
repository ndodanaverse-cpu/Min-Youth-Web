/**
 * Public site config for static HTML pages (PHP pages also set this via portal_url()).
 * Keep portalUrl in sync with config/site.php (or MINYOUTH_PORTAL_URL).
 */
window.MINYOUTH_PORTAL_URL = window.MINYOUTH_PORTAL_URL || 'http://127.0.0.1:8000';
